#include "main.h"

/**
 * _abs - if c is lowercase .
 * @number:parameter
 * Return: number if greater or equal to 0
 *   -number if not
 */
int _abs(int number);
{
	if (number >= 0)
		return (number);
	else
		return (-number);
}
