#include <stdio.h>
/**
 * main - outputs numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch;
for (ch = 48; ch <= 58; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
